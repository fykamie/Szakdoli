import { IsShowMinusbalancePipe } from './is-show-minusbalance.pipe';

describe('IsShowMinusbalancePipe', () => {
  it('create an instance', () => {
    const pipe = new IsShowMinusbalancePipe();
    expect(pipe).toBeTruthy();
  });
});
