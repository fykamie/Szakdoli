import { IsShowPlusbalancePipe } from './is-show-plusbalance.pipe';

describe('IsShowPlusbalancePipe', () => {
  it('create an instance', () => {
    const pipe = new IsShowPlusbalancePipe();
    expect(pipe).toBeTruthy();
  });
});
