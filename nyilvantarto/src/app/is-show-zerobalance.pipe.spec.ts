import { IsShowZerobalancePipe } from './is-show-zerobalance.pipe';

describe('IsShowZerobalancePipe', () => {
  it('create an instance', () => {
    const pipe = new IsShowZerobalancePipe();
    expect(pipe).toBeTruthy();
  });
});
